@model List<User>

<h2>List of users</h2>

<ul>
    @foreach (var user in Model)
    {
        <li>
            @user.UserName - @user.Email
            <a asp-action="EditUser" asp-route-id="@user.Id">Edit</a>
        </li>
    }
</ul>